cc      = emcc
cxx     = em++
cflags  = -g3 -s WASM=1
ldflags = -g3 -s WASM=1 --embed-file profiles --emrun
exe     = .html
out     = out/emscripten$mode

rule run
    command = $disabled && touch $out || emrun $in > $out

include build/common

disabled = (uname | grep -qv Linux)
