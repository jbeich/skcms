mode          = .avx512
extra_cflags  = -arch x86_64 -march=skylake-avx512 -DSKCMS_NO_RUNTIME_CPU_DETECTION
extra_ldflags = -arch x86_64
include ninja/clang

disabled = (uname | grep -q Darwin && sysctl machdep.cpu | grep -qv AVX512F || grep '^flags' /proc/cpuinfo | grep -vq avx512f)
