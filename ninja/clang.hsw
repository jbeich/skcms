mode          = .hsw
extra_cflags  = -arch x86_64 -march=haswell -DSKCMS_NO_RUNTIME_CPU_DETECTION
extra_ldflags = -arch x86_64
include ninja/clang

disabled = (uname | grep -q Darwin && sysctl machdep.cpu | grep -qv AVX2 || grep '^flags' /proc/cpuinfo | grep -vq avx2)
